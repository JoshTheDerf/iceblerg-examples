### Using Custom Template and Markup Languages with iceblerg.

This is best explained by code. Thankfully, it's very simple. Take a look at `build.js` to see how to do it.

Run `node build.js` to generate the blog.
